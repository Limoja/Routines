=== Evidence Files ===

- e2e-full.log — Full E2E test: signup → init → next → outcomes → next
- e2e-law3-proper.log — Law 3 enforcement test with proper score > 0.6 setup
- e2e-law3.log — Initial Law 3 test (score=0.6, no trigger)
- e2e-depth-edge.log — Depth selection + auth edge cases
- outcomes-debug.log — Outcomes endpoint debug (confirmed 500 on non-UUID session_id)
